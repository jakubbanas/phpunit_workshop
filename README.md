# phpunit_workshop

## How to run:
  * run in Docker environment
  >`composer docker` 

## How to run tests:
  * run tests in Docker environment
  >`composer docker-test`
  
  * generate test coverage
  >`composer coverage`

  * run mutation tests
  >`composer infection`