# phpunit_workshop

## How to run:
  >`composer run`
  
   OR run in Docker environment
  >`composer docker` 

## How to run tests:
  >`composer test` 

   OR run in Docker environment
  >`composer docker-test`